# 正確で一意に定まる表現を使用する

* 同じ対象を複数の用語で不必要に言い換えない
* 対象が一意に特定できる用語を選択して使用する

# 曖昧な用語を使用しない

他に適切な技術用語があるにも関わらず、技術的に定義されていない曖昧な表現を使用することを禁止します。

* 「寄せる」「整理する」「汚す」「固定する」「つながる経路」「契約」「壊す」「見失う」

# 定量的に示せるにも関わらず、無意味に曖昧な表現を使用することを禁止します。

* 「小さく」「大きく」

# 実装者目線での造語を使用しない

実装の細部を知らないと解読不可能となるような、実装者本位な造語の使用を禁止します。
単語が長くなったとしても、一般的な””定義済み””の技術用語の組み合わせのみを使用してください。

# DO NOT条件を文書に残さない

* ユーザーから「〇〇をしないでください」と指示された後に、文書やコードに「〇〇はしません」という記録を残すことを禁止します

# 手順および並列関係を混ぜない

* 1. 2. 3. ... という番号付きリストは、その順番で示すことに意味がある項目（手順、フローなど）に限定する。
* 「*」「-」で示す箇条書きは、それぞれの項目が並列関係にあることを確認して使用する。
  並列関係にない場合は、箇条書きのレベルを調整して入れ子にしたり、そもそも箇条書きであることが不適切である。

# プログラムを書く上での遵守事項

- 後方互換性を維持しない。互換性レイヤー、フォールバック、マイグレーションを追加する代わりに、古いパスを削除してください。
- 現在の要件を完全に満たす最もシンプルな実装を選択する。先読みの抽象化、設定、間接化を避ける。
- システムを階層状に成長させる。End-to-end で動作する最小バージョンから始め、動作する製品の上に新しい機能を1つずつ追加する。動作中の製品を未完了の複雑さと交換しない。
- コンポーネントをモジュール化し、関心事を明確に分離する。
- 全体の複雑さを減らし、信頼性を向上させる場合は、整備された確立されたライブラリを優先する。明確な理由がない限り、共通機能を再実装しない。
- 独自の処理を実装したりパッケージを追加したりする前に、プロジェクトの既存の依存に頼る。ドキュメントや型を確認せずに、ライブラリが何らかの機能を欠いていると仮定しない。
- 長期的なアーキテクチャ的意思決定を行う。今だけのために働き、後で置き換える meant な暫定策を受けない。

---

# Repository Instructions

このファイルはリポジトリ全体に適用する。

## 最初に読む文書

1. `docs/developer/DEVELOPMENT.md`
2. `README.md`
3. `docs/USER_GUIDE.md`

開発ガイドとこのファイルに記載された回帰条件を優先する。

## 変更してはいけない前提

- 推論はローカルモデルだけで実行する。オンライン推論APIへ接続しない。
- 通常利用は`EarCopyAssist.exe`を直接起動するだけとし、利用者へ別サービスの
  手動起動を要求しない。
- MuScriptorのsmall／medium／largeを同じ仕組みで登録・利用可能にする。
- モデル重みをGitへ追加しない。`models/`と
  `*.safetensors`、`*.ckpt`、`*.pt`、`*.pth`、`*.onnx`、`*.th`、
  `*.gguf`は除外対象である。分離モデル重みも`models/`から読む。
- UIには操作に必要な説明だけを表示し、宣伝文や抽象的なキャッチコピーを追加しない。
- 既存のユーザー変更を破棄しない。

## 実装時の確認

- Python変更後は`uv run pytest -q`を実行する。
- Renderer／Electron変更後は`app`で`npm test -- --run`と
  `npm run typecheck`を実行する。
- 単体EXE作成とelectron-builderによるパッケージ化は一時停止中である。
  ユーザーから明示的な再開指示があるまで`npm run dist:win`を実行しない。
- `git diff --check`を通し、テストしていない内容を「確認済み」と記載しない。
- モデル混入は`git ls-files`と`git check-ignore -v <path>`で確認する。

## 重要な回帰条件

- Electron mainがランダムなloopbackポートで内蔵Pythonバックエンドを起動し、
  preload経由で接続情報をRendererへ渡す構成を維持する。
- Rendererからloopback以外のAPI URLを受け付けない。
- 原音Wave再生をSoundFontレンダリング経路へ戻さない。
- 入力Waveを44.1 kHz固定として扱わない。8／48／96 kHzの検証を維持する。
- ピアノロールの横軸、小節線、拍線、クオンタイズはBPMと拍位相に基づく。
- Soloが1件以上ある場合、再生だけでなくピアノロール表示もSoloパートだけにする。
- 原音、採譜結果、左右比較を選択した場合はMute／Soloを各モードの初期状態へ変更する。
- 左右比較で分離音源が存在する場合は、対応する分離音源と採譜トラックのMute／Soloを
  同時に変更する。分離音源が存在しない場合は原音全体の再生状態を維持する。
- Solo中のMute操作は、採譜結果モードでSolo対象の採譜トラックに限り許可する。
- ドラムノートは短くても14 px以上の表示幅・選択領域を維持する。
- 音程ノートの10 ms以下と10秒超は採譜全体の失敗にせず無効化し、直後の
  同一パート・同一音程リトリガーも無効化する。ドラムだけは最小10 msへ補正する。

## 公開

- EarCopy Assist本体はMIT Licenseである。ライセンスと著作権者表記を独断で
  変更しない。
- バイナリ公開前に`docs/developer/DISTRIBUTION.md`を読む。
- BS-RoFormer SW Fixed重みは配布物へ同梱しない。
- Windows版公開時はLGPL版FFmpegと同じ配布場所から
  `app/release-sources/`の対応ソースを取得できるようにする。
